# Implementation prompt for Codex — AutoxVision

Proceed with implementation using the accepted plan and the project docs.

Requirements:

- Initialize/use local git.
- Implement the complete MVP, not just a scaffold.
- Work in slices and commit meaningful milestones.
- Keep dependencies minimal; no backend, no CDN imports, no required preprocessing.
- Use direct local video playback through browser APIs.
- Implement the UI, trial engine, feedback, scoring/high score, controls, presets, modes, notes autosave, export/import, and tests described in `SPEC.md` and `ACCEPTANCE.md`.
- Run `npm install`, `npm test`, `npm run build`, and `npm run dev` as applicable.
- Visually inspect the localhost app, revise issues, and repeat until it meets acceptance criteria.
- Update `WORKLOG.md` with decisions, commits, commands, test results, visual inspection notes, and any remaining limitations.

Hand back only after the app is complete and verified. Include the run/build/test commands and the final git status.
